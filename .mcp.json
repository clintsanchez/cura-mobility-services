{
  "_comment": "Cura Mobility Services. The Novamira WordPress MCP server (novamira-cura-mobility-lo -> https://cura-mobility.local/wp-json/mcp/novamira) is registered in LOCAL scope (~/.claude.json via `claude mcp add-json -s local`) so the application password never lands in git. Re-add it that way on a new machine.",
  "mcpServers": {}
}
